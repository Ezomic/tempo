[pytest]
testpaths = tests
# garminconnect 0.3.2 still uses pydantic's class-based Config, which is
# deprecated but not ours to fix; it would otherwise drown the output.
filterwarnings =
    ignore::DeprecationWarning
